# Multi-agent system (auto-added by init-agent-env)
# Remove this line if you want to commit planning files to version control
.claude-agents/

# DevSpace cache and logs (auto-added by init-agent-env)
.devspace/
